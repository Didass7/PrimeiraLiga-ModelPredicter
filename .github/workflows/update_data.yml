name: Pipeline de Atualização de Dados (Fim de Época)

on:
  schedule:
    # Corre todos os verões: Dia 1 de Julho de cada ano às 08:00 UTC
    - cron: '0 8 1 7 *'
  workflow_dispatch: # Permite-te iniciar o pipeline manualmente no browser sempre que quiseres

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      # Permissões necessárias para gravar as atualizações de volta no repositório
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip' # Otimiza o build fazendo cache das dependências do pip

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run ETL Pipeline
        run: python pipeline/run_all.py

      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Auto-update: integração de dados de fim de época (24/25 e 25/26)"
          file_pattern: "Datasets/*.csv"
